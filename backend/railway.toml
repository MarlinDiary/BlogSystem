[build]
builder = "nixpacks"
buildCommand = "npm install --no-cache --production --prefer-offline"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
numReplicas = 1

[env]
NODE_ENV = "production"
PORT = "3000"

[[volume]]
source = "uploads"
path = "/data/uploads" 